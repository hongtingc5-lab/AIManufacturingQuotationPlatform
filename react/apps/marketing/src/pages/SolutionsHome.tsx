import { VerifiedHtmlPage } from './VerifiedHtmlPage'

/** /solutions — industrial FDM solutions hub from verified HTML. */
export default function SolutionsHome() {
  return <VerifiedHtmlPage localeKey="pages.solutions.home" fallbackBodyClasses={['solution-page']} />
}
