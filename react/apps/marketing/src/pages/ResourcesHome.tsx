import { VerifiedHtmlPage } from './VerifiedHtmlPage'

/** /resources — resource hub from verified HTML. */
export default function ResourcesHome() {
  return <VerifiedHtmlPage localeKey="pages.resources.home" fallbackBodyClasses={['resource-page']} />
}
